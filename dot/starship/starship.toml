# Set command timeout to prevent delays
command_timeout = 5000

[directory]
read_only = ""  # Disable the lock sign

[directory.substitutions]
"/storage/emulated/0/0a/doc" = "󰈙 "
"/sdcard/Download" = " "
"/sdcard/Music" = " "
"/storage/emulated/0/0a/pho" = " "
"/storage/emulated/0/-" = " "  # Preserve directory substitution
